import { IUsuarioRequestEntity } from './iusuario-request-entity';

describe('IUsuarioRequestEntity', () => {
  it('deve ser criado uma instancia', () => {
    expect(new IUsuarioRequestEntity()).toBeTruthy();
  });
});
